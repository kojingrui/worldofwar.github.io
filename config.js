const CONFIG = {
    mapSize: 8, // 地图大小 8x8
    
    // 渲染尺寸
    tileSize: 70, // 地块大小
    edgeThickness: 10, // 边境的厚度
    
    // 概率配置
    terrainProb: [0.5, 0.6, 0.7, 0.8, 1.0], // 平原, 高山, 沙漠, 湖泊, 森林
};